<!--
  此文件是由模板文件 ".dtpl/component/$rawModuleName.wxml.dtpl" 生成的，你可以自行修改模板

  注意：
  1. 如果没有语法高亮，且你使用的是 vscode 编辑器的话，建义在 vscode 插件库中安装 minapp-vscode 插件
-->

<i-card title="第{{campProp.order}}期训练营:{{campProp.content}}">
    <view slot="content">{{campProp.description}}</view>
    <view slot="footer" class="i-card-footer">训练营结束时间：{{campProp.endTime}}</view>
</i-card>
